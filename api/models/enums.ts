export enum EmployeeType {
  EXTERNAL = "EXTERNO",
  INTERNAL = "INTERNO",
}

export enum CostType {
  HOURLY = "HORA",
  MONTHLY = "MENSUAL",
}

export enum InternalCostType {
  INTEGRAL = "INTEGRAL",
  ORDINARY = "ORDINARIO",
}

export enum Profiles {
  ADMINISTRATOR = 1,
  CONTROLLER = 2,
  AREA_MANAGER = 3,
  PROYECT_MANAGER = 4,
  HUMAN_RESOURCES = 5,
  SALES = 6,
  CONSULTANT = 7,
}

export enum TipoIdentificacion {
  CC = "CC",
  CE = "CE",
  PA = "PA",
  RC = "RC",
  TI = "TI",
}

export enum TipoSangre {
  "A+" = "A+",
  "A-" = "A-",
  "B+" = "B+",
  "B-" = "B-",
  "AB+" = "AB+",
  "AB-" = "AB-",
  "C+" = "C+",
  "C-" = "C-",
  "O+" = "O+",
  "O-" = "O-",
}
